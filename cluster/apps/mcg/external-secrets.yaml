---
# App secrets from 1Password. `mcg-app` item fields:
#   database-url    — postgres://mcg:<pw>@postgres.infra.asn.casa:5432/mcg_production
#   rails-master-key — contents of config/master.key
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mcg-app
  namespace: mcg
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "24h"
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: mcg-app
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: "mcg-app/database-url"
    - secretKey: RAILS_MASTER_KEY
      remoteRef:
        key: "mcg-app/rails-master-key"
---
# Pull secret for ghcr.io — shared `ghcr-pull` 1Password item (username +
# token with read:packages), same pattern as the pmn registry secrets.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-pull
  namespace: mcg
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "24h"
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: ghcr-pull
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        ".dockerconfigjson": '{"auths":{"ghcr.io":{"username":"{{ .username }}","password":"{{ .token }}"}}}'
  data:
    - secretKey: username
      remoteRef:
        key: "ghcr-pull/username"
    - secretKey: token
      remoteRef:
        key: "ghcr-pull/token"
