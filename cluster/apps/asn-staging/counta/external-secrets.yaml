---
# App secrets from 1Password. `counta-staging` item fields (database-url
# written by scripts/provision_app_db.sh counta staging):
#   database-url     — postgres://counta_staging:<pw>@postgres.infra.asn.casa:5432/counta_staging
#   rails-master-key — contents of config/master.key
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: counta-app
  namespace: asn-staging
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "24h"
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: counta-app
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: "counta-staging/database-url"
    - secretKey: RAILS_MASTER_KEY
      remoteRef:
        key: "counta-staging/rails-master-key"
