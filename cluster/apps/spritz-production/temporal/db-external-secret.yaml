---
# Credentials for the managed Postgres (postgres.infra.asn.casa). Role
# temporal_spritz_production needs CREATEDB — the schema-setup hook creates the
# temporal_spritz_production and temporal_spritz_production_visibility databases on first sync.
# Provisioned via scripts/provision_app_db.sh temporal_spritz production --skip-db
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: temporal-db
  namespace: spritz-production
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "24h"
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: temporal-db
  data:
    - secretKey: username
      remoteRef:
        key: "temporal-spritz-production/database-username"
    - secretKey: password
      remoteRef:
        key: "temporal-spritz-production/database-password"
