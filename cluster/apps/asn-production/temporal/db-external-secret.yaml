---
# Credentials for the managed Postgres (postgres.infra.asn.casa). Role
# temporal_asn_production needs CREATEDB — the schema-setup hook creates the
# temporal_asn_production and temporal_asn_production_visibility databases on first sync.
# Provisioned via scripts/provision_app_db.sh temporal_asn production --skip-db
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: temporal-db
  namespace: asn-production
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "24h"
  secretStoreRef:
    name: onepassword-backend
    kind: ClusterSecretStore
  target:
    name: temporal-db
  data:
    - secretKey: username
      remoteRef:
        key: "temporal-asn-production/database-username"
    - secretKey: password
      remoteRef:
        key: "temporal-asn-production/database-password"
