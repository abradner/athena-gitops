---
# mcg web (production). Single process — no background jobs yet. The image
# entrypoint runs `rails db:prepare` before the server, so migrations apply
# on rollout.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcg-web
  namespace: asn-production
spec:
  # Cutover 2026-08-25: the old VM that served this app is dead — port closed,
  # mcg.rest was serving 503. This targets the same `mcg_production` database
  # on postgres.infra.asn.casa the VM used, so data carries over; db:prepare
  # in the entrypoint applies any pending migrations with no live app to race.
  replicas: 1
  selector:
    matchLabels:
      app: mcg-web
  template:
    metadata:
      labels:
        app: mcg-web
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: web
          image: ghcr.io/abradner/mcg:sha-40c65c86ab3f7161c3bc5b211ec4d3b74937571a
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - secretRef:
                name: mcg-app
          readinessProbe:
            httpGet:
              path: /up
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /up
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mcg-web
  namespace: asn-production
spec:
  selector:
    app: mcg-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcg
  namespace: asn-production
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: default
      sectionName: https-mcg-rest
  hostnames:
    - "mcg.rest"
  rules:
    - backendRefs:
        - name: mcg-web
          port: 80
